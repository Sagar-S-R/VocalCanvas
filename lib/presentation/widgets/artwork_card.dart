// ArtworkCard: Common widget
import 'package:flutter/material.dart';

class ArtworkCard extends StatelessWidget {
  const ArtworkCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(child: Text('Artwork Card'));
  }
}
