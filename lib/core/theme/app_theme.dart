// AppTheme: Your app's theme data
import 'package:flutter/material.dart';

class AppTheme {
  static ThemeData get theme =>
      ThemeData(primarySwatch: Colors.blue, brightness: Brightness.dark);
}
